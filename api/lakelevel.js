export default async function handler(req,res){res.setHeader('Access-Control-Allow-Origin','*');let level=null;try{const t=await (await fetch('https://www.bizer.com/bzt/bztmap.shtml',{headers:{'User-Agent':'Mozilla/5.0'}})).text();const m=t.match(/Lake Level[^0-9]{0,30}([5][0-9]{2}\.[0-9]{1,2})/i);if(m)level=parseFloat(m[1]);}catch{}if(!level)level=504.10;res.json({level:Number(level.toFixed(2)),gageHeight:Number((level-500).toFixed(2)),time:new Date().toISOString(),source:'Bizer LIVE',fullPool:504.32,live:true});}